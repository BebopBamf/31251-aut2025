#include "two_sum.hpp"

std::pair<int, int> twoSum(const std::vector<int>& vec, int target) {
	std::ignore = vec;
	std::ignore = target;

	DEBUG_LOG("Test Logging!");
	DEBUG_LOG_LIST(vec);

	std::unordered_set vals{};

	for (const int& n : vec) {
		if (vals.contains)
	}

	return {0, 0};
}
